package com.example.helloworld;

public interface KeyVaultClient {

	/*
	 * SecretClient getSecretClient();
	 * 
	 * default KeyVaultSecret getSecret(String key) { KeyVaultSecret secret=null;
	 * try { secret = getSecretClient().getSecret(key); } catch (Exception ex) {
	 * //throw new
	 * NoSuchElementException(String.format("Unable to retrieve %s secret", key),
	 * ex); } return secret; }
	 */
}
